import { FormEvent, ReactNode, useEffect, useMemo, useState } from 'react'
import './source-filter.css'
import {
  Activity, CalendarClock, Check, CheckCircle2, ChevronDown, CircleAlert, Database, Eye, Gauge,
  KeyRound, LayoutDashboard, LockKeyhole, LogOut, Menu as MenuIcon, Play, Plus,
  RefreshCw, Search, Server, ShieldCheck, TableProperties, Trash2, Pencil, Download, Upload, BookOpen, Users, X, Zap,
} from 'lucide-react'
import { api, ApiError, fmt } from './api'
import type { Capabilities, Job, Menu, Permission, Role, Run, Source, User } from './types'

type Page = 'dashboard' | 'sources' | 'metadata' | 'registered' | 'jobs' | 'users' | 'roles'
const nav: { id: Page; label: string; icon: typeof Gauge; group: string }[] = [
  { id: 'dashboard', label: '대시보드', icon: LayoutDashboard, group: 'OVERVIEW' },
  { id: 'sources', label: 'DB 접속 관리', icon: Database, group: 'DATA MANAGEMENT' },
  { id: 'metadata', label: '스키마 정보', icon: TableProperties, group: 'DATA MANAGEMENT' },
  { id: 'registered', label: '등록 메타 관리', icon: Pencil, group: 'DATA MANAGEMENT' },
  { id: 'jobs', label: '수집 스케줄', icon: CalendarClock, group: 'AUTOMATION' },
  { id: 'users', label: '사용자 관리', icon: Users, group: 'ADMINISTRATION' },
  { id: 'roles', label: '역할 및 권한', icon: ShieldCheck, group: 'ADMINISTRATION' },
]

const dbNames: Record<string, string> = { postgresql: 'PostgreSQL', mysql: 'MySQL', mariadb: 'MariaDB', mssql: 'SQL Server', oracle: 'Oracle', sqlite: 'SQLite' }
const dbPorts: Record<string, number> = { postgresql: 5432, mysql: 3306, mariadb: 3306, mssql: 1433, oracle: 1521 }
const fmtBytes = (value?: number | null) => {
  if (value == null) return '—'
  const units = ['B', 'KB', 'MB', 'GB', 'TB']
  let size = Math.abs(value), unit = 0
  while (size >= 1024 && unit < units.length - 1) { size /= 1024; unit++ }
  return `${value < 0 ? '-' : ''}${size.toFixed(unit ? 1 : 0)} ${units[unit]}`
}

function useLoad<T>(path: string, fallback: T, deps: unknown[] = []) {
  const [data, setData] = useState<T>(fallback)
  const [loading, setLoading] = useState(true)
  const load = () => { setLoading(true); api<T>(path).then(setData).finally(() => setLoading(false)) }
  useEffect(load, deps)
  return { data, loading, reload: load, setData }
}

function Toast({ message, onClose }: { message: string; onClose: () => void }) {
  useEffect(() => { const id = setTimeout(onClose, 3500); return () => clearTimeout(id) }, [message])
  return <div className="toast"><Check size={17} />{message}</div>
}

function Modal({ title, subtitle, children, onClose, wide }: { title: string; subtitle?: string; children: ReactNode; onClose: () => void; wide?: boolean }) {
  return <div className="modal-backdrop" onMouseDown={e => e.target === e.currentTarget && onClose()}>
    <div className={`modal ${wide ? 'modal-wide' : ''}`}>
      <div className="modal-head"><div><h2>{title}</h2>{subtitle && <p>{subtitle}</p>}</div><button className="icon-btn" onClick={onClose}><X size={20}/></button></div>
      {children}
    </div>
  </div>
}

function Status({ value }: { value: string }) {
  const labels: Record<string, string> = { connected: '연결됨', unchecked: '확인 전', failed: '실패', success: '성공', running: '실행 중', queued: '대기' }
  return <span className={`status status-${value}`}><i/>{labels[value] || value}</span>
}

function Empty({ title, text, icon: Icon = Database }: { title: string; text: string; icon?: typeof Database }) {
  return <div className="empty"><div className="empty-icon"><Icon size={25}/></div><h3>{title}</h3><p>{text}</p></div>
}

function Login({ onLogin }: { onLogin: (user: User) => void }) {
  const [email, setEmail] = useState('admin@example.com')
  const [password, setPassword] = useState('Admin123!')
  const [error, setError] = useState('')
  const [busy, setBusy] = useState(false)
  const submit = async (e: FormEvent) => {
    e.preventDefault(); setBusy(true); setError('')
    try {
      const result = await api<{ access_token: string; user: User }>('/api/auth/login', { method: 'POST', body: JSON.stringify({ email, password }) })
      localStorage.setItem('metavault_token', result.access_token); onLogin(result.user)
    } catch (e) { setError((e as Error).message) } finally { setBusy(false) }
  }
  return <main className="login-page">
    <section className="login-brand"><div className="brand"><div className="brand-mark"><Database size={22}/></div><b>Meta<span>Vault</span></b></div><div className="login-copy"><p className="eyebrow">ENTERPRISE DATA INTELLIGENCE</p><h1>흩어진 데이터 구조를<br/><em>하나의 자산으로.</em></h1><p>다양한 데이터베이스의 스키마를 안전하게 수집하고,<br/>조직의 데이터 자산을 한곳에서 관리하세요.</p><div className="feature-row"><span><Zap/>자동화 수집</span><span><KeyRound/>다중 인증</span><span><ShieldCheck/>세밀한 권한</span></div></div><div className="orb orb-one"/><div className="orb orb-two"/></section>
    <section className="login-form-wrap"><form className="login-card" onSubmit={submit}><div className="mobile-brand brand"><div className="brand-mark"><Database size={20}/></div><b>Meta<span>Vault</span></b></div><p className="eyebrow">WELCOME BACK</p><h2>관리자 로그인</h2><p className="muted">계속하려면 계정 정보를 입력하세요.</p>{error && <div className="alert"><CircleAlert size={16}/>{error}</div>}<label>이메일 주소<input value={email} onChange={e => setEmail(e.target.value)} type="email" required/></label><label>비밀번호<input value={password} onChange={e => setPassword(e.target.value)} type="password" required/></label><button className="primary login-button" disabled={busy}>{busy ? <RefreshCw className="spin" size={18}/> : <LockKeyhole size={18}/>}로그인</button><p className="login-hint">초기 계정은 환경변수로 변경할 수 있습니다.</p></form></section>
  </main>
}

function Dashboard({ go }: { go: (p: Page) => void }) {
  const { data, loading } = useLoad<any>('/api/dashboard', { sources: 0, active_jobs: 0, objects: 0, failed_runs: 0, recent_runs: [] }, [])
  const cards = [
    ['등록 데이터 소스', data.sources, '개', Database, 'blue'], ['활성 수집 작업', data.active_jobs, '개', CalendarClock, 'violet'],
    ['누적 수집 객체', data.objects, '개', TableProperties, 'mint'], ['실패한 실행', data.failed_runs, '건', CircleAlert, 'coral'],
  ] as const
  return <><PageHead eyebrow="OVERVIEW" title="데이터 운영 현황" description="등록된 데이터 소스와 수집 작업의 상태를 한눈에 확인하세요." action={<button className="primary" onClick={() => go('sources')}><Plus size={17}/>DB 연결 추가</button>}/>
    <div className="stats">{cards.map(([label, value, unit, Icon, color]) => <div className="stat-card" key={label}><div className={`stat-icon ${color}`}><Icon size={21}/></div><div><p>{label}</p><strong>{loading ? '—' : value.toLocaleString()}<small>{unit}</small></strong></div></div>)}</div>
    <div className="grid-2"><section className="panel"><PanelHead title="최근 수집 실행" text="최신 8건의 작업 결과"/><div className="run-list">{data.recent_runs.length ? data.recent_runs.map((run: Run) => <div className="run-row" key={run.id}><div className="run-dot"><Activity size={16}/></div><div><b>수집 작업 #{run.job_id}</b><p>{fmt(run.started_at)} · {run.object_count}개 객체</p></div><Status value={run.status}/></div>) : <Empty title="아직 실행 기록이 없습니다" text="수집 작업을 등록하고 첫 실행을 시작해 보세요." icon={Activity}/>}</div></section>
    <section className="panel quick-panel"><PanelHead title="빠른 시작" text="3단계로 메타데이터 수집을 시작하세요."/><ol className="steps"><li className={data.sources ? 'done' : ''}><span>{data.sources ? <Check/> : '1'}</span><div><b>데이터베이스 연결</b><p>접속 정보와 인증 방식을 등록합니다.</p></div></li><li className={data.active_jobs ? 'done' : ''}><span>{data.active_jobs ? <Check/> : '2'}</span><div><b>수집 스케줄 생성</b><p>대상 스키마와 실행 주기를 지정합니다.</p></div></li><li><span>3</span><div><b>수집 결과 탐색</b><p>스키마와 테이블 구조를 검색합니다.</p></div></li></ol></section></div>
  </>
}

function PageHead({ eyebrow, title, description, action }: { eyebrow: string; title: string; description: string; action?: ReactNode }) {
  return <header className="page-head"><div><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p>{description}</p></div>{action}</header>
}
function PanelHead({ title, text, action }: { title: string; text?: string; action?: ReactNode }) { return <div className="panel-head"><div><h3>{title}</h3>{text && <p>{text}</p>}</div>{action}</div> }

function SourceForm({ onClose, onDone, supportedDbTypes, source }: { onClose: () => void; onDone: () => void; supportedDbTypes: string[]; source?: Source }) {
  const [form,setForm] = useState<any>(() => ({ name: '', db_type: 'postgresql', host: 'localhost', port: 5432, database: '', username: '', ssl_ca_cert: '', ssl_cert: '', ssl_key: '', ssh_enabled: false, ssh_port: 22, ssh_auth_type: 'private_key', ...(source || {}), ssl_enabled: Boolean(source?.options?.ssl_enabled), ssl_mode: source?.options?.sslmode || 'require', password: '', ssh_password: '', ssh_private_key: '', ssh_private_key_passphrase: '' }))
  const [error,setError] = useState(''); const [busy,setBusy] = useState(false); const [testing,setTesting] = useState(false); const [testResult,setTestResult] = useState('')
  const set = (key: string, value: any) => setForm((f: any) => ({ ...f, [key]: value }))
  const cleanPayload = () => { const payload = { ...form }; const options = { ...(form.options || {}) }; if (form.db_type === 'postgresql') options.sslmode = form.ssl_mode || 'require'; else delete options.sslmode; payload.options = options; delete payload.ssl_mode; delete payload.id; delete payload.status; delete payload.last_tested_at; delete payload.created_at; for (const key of ['password','service_account_json','ssh_password','ssh_private_key','ssh_private_key_passphrase','ssl_ca_cert','ssl_cert','ssl_key']) if (!payload[key]) delete payload[key]; return payload }
  const testConnection = async () => { setTesting(true); setError(''); setTestResult(''); try { await api(source ? `/api/sources/${source.id}/test-edit` : '/api/sources/test', { method: 'POST', body: JSON.stringify(cleanPayload()) }); setTestResult('연결 테스트에 성공했습니다. 아직 저장되지는 않았습니다.') } catch (e) { setError((e as Error).message) } finally { setTesting(false) } }
  const submit = async (e: FormEvent) => { e.preventDefault(); setBusy(true); setError(''); try { await api(source ? `/api/sources/${source.id}` : '/api/sources', { method: source ? 'PUT' : 'POST', body: JSON.stringify(cleanPayload()) }); onDone() } catch (e) { setError((e as Error).message) } finally { setBusy(false) } }
  return <Modal title={source?'데이터베이스 연결 수정':'데이터베이스 연결 추가'} subtitle="접속 정보는 암호화되어 안전하게 저장됩니다." onClose={onClose} wide><form onSubmit={submit}><div className="form-section"><h4>기본 접속 정보</h4><div className="form-grid"><label>연결 이름<input required placeholder="예: 운영계 PostgreSQL" value={form.name} onChange={e => set('name', e.target.value)}/></label><label>데이터베이스 종류<select value={form.db_type} onChange={e => { const type = e.target.value; setForm({ ...form, db_type: type, port: dbPorts[type] || undefined }) }}>{Object.entries(dbNames).filter(([value]) => supportedDbTypes.includes(value)).map(([v,l]) => <option value={v} key={v}>{l}</option>)}</select></label>{form.db_type === 'sqlite' ? <label className="span-2">DB 파일 경로<input required placeholder="C:\data\sample.db" value={form.database} onChange={e => set('database', e.target.value)}/></label> : <><label>호스트<input required value={form.host} onChange={e => set('host', e.target.value)}/></label><label>포트<input required type="number" value={form.port || ''} onChange={e => set('port', Number(e.target.value))}/></label><label>데이터베이스 / 서비스명<input required value={form.database} onChange={e => set('database', e.target.value)}/></label><label>사용자명<input required value={form.username} onChange={e => set('username', e.target.value)}/></label><label className="span-2">비밀번호<input type="password" value={form.password} onChange={e => set('password', e.target.value)}/></label></>}</div></div>
    {form.db_type !== 'sqlite' && <div className="form-section"><div className="toggle-row"><div><h4>SSL/TLS 암호화</h4><p>인증서 기반으로 안전하게 데이터베이스에 연결합니다. Aiven MySQL은 CA 인증서가 필요합니다.</p></div><button type="button" className={`toggle ${form.ssl_enabled ? 'on' : ''}`} onClick={() => set('ssl_enabled', !form.ssl_enabled)}><i/></button></div>{form.ssl_enabled && <div className="form-grid tunnel"><label className="span-2">CA 인증서 (PEM)<textarea required={!source} rows={7} placeholder="-----BEGIN CERTIFICATE-----" value={form.ssl_ca_cert} onChange={e => set('ssl_ca_cert', e.target.value)}/><small>서비스 제공자가 내려받은 CA 인증서 내용을 붙여넣으세요.</small></label>{form.db_type === 'postgresql' && <label>SSL 모드<select value={form.ssl_mode || 'require'} onChange={e => set('ssl_mode', e.target.value)}><option value="require">require (암호화, 인증서 검증 안 함)</option><option value="verify-ca">verify-ca (CA 인증서 검증)</option><option value="verify-full">verify-full (CA + 호스트명 검증)</option></select><small>verify-full은 인증서의 호스트명과 DB 호스트가 일치해야 합니다.</small></label>}<label className="span-2">클라이언트 인증서 (선택)<textarea rows={5} placeholder="-----BEGIN CERTIFICATE-----" value={form.ssl_cert} onChange={e => set('ssl_cert', e.target.value)}/></label><label className="span-2">클라이언트 키 (선택)<textarea rows={5} placeholder="-----BEGIN PRIVATE KEY-----" value={form.ssl_key} onChange={e => set('ssl_key', e.target.value)}/></label></div>}</div>}
    {form.db_type !== 'sqlite' && <div className="form-section"><div className="toggle-row"><div><h4>SSH 터널 사용</h4><p>점프 서버를 통해 사설망 DB에 안전하게 접속합니다.</p></div><button type="button" className={`toggle ${form.ssh_enabled ? 'on' : ''}`} onClick={() => set('ssh_enabled', !form.ssh_enabled)}><i/></button></div>{form.ssh_enabled && <div className="form-grid tunnel"><label>SSH 호스트<input required value={form.ssh_host || ''} onChange={e => set('ssh_host', e.target.value)}/></label><label>SSH 포트<input type="number" value={form.ssh_port} onChange={e => set('ssh_port', Number(e.target.value))}/></label><label>SSH 사용자<input required value={form.ssh_username || ''} onChange={e => set('ssh_username', e.target.value)}/></label><label>인증 방식<select value={form.ssh_auth_type} onChange={e => set('ssh_auth_type', e.target.value)}><option value="private_key">개인키</option><option value="password">비밀번호</option></select></label>{form.ssh_auth_type === 'private_key' ? <><label className="span-2">개인키 (PEM)<textarea required={!source} rows={5} placeholder="-----BEGIN OPENSSH PRIVATE KEY-----" value={form.ssh_private_key || ''} onChange={e => set('ssh_private_key', e.target.value)}/></label><label className="span-2">키 암호 (선택)<input type="password" value={form.ssh_private_key_passphrase || ''} onChange={e => set('ssh_private_key_passphrase', e.target.value)}/></label></> : <label className="span-2">SSH 비밀번호<input required={!source} type="password" value={form.ssh_password || ''} onChange={e => set('ssh_password', e.target.value)}/></label>}</div>}</div>}
    {error && <div className="alert"><CircleAlert size={16}/>{error}</div>}{testResult && <div className="test-success"><CheckCircle2 size={16}/>{testResult}</div>}<div className="modal-actions"><button type="button" className="ghost" onClick={onClose}>취소</button><button type="button" className="ghost" disabled={busy||testing} onClick={testConnection}>{testing && <RefreshCw className="spin" size={16}/>}연결 테스트</button><button className="primary" disabled={busy||testing}>{busy && <RefreshCw className="spin" size={16}/>}연결 저장</button></div></form></Modal>
}

function ImportGuide({ onClose }: { onClose: () => void }) {
  const yamlExample = `connections:
  - name: 운영 PostgreSQL
    db_type: postgresql
    host: db.example.com
    port: 5432
    database: production
    username: metadata_reader
    password: \${PROD_DB_PASSWORD}

  - name: 운영 Oracle
    db_type: oracle
    host: oracle.example.com
    port: 1521
    database: ORCL
    username: metadata_reader
    password: \${ORACLE_DB_PASSWORD}
    options:
      service_name: ORCL
    ssh:
      enabled: true
      host: bastion.example.com
      port: 22
      username: deploy
      auth_type: private_key
      private_key: \${BASTION_PRIVATE_KEY}`
  const jsonExample = `{
  "connections": [
    {
      "name": "운영 PostgreSQL",
      "db_type": "postgresql",
      "host": "db.example.com",
      "port": 5432,
      "database": "production",
      "username": "metadata_reader",
      "password": "\${PROD_DB_PASSWORD}"
    }
  ]
}`
  return <Modal title="DB 접속 Import 가이드" subtitle="YAML 또는 JSON으로 여러 연결을 한 번에 등록합니다." onClose={onClose} wide><div className="guide-content"><h4>지원 형식</h4><p><code>connections</code> 배열을 최상위에 두고 각 DB 접속 정보를 입력합니다. 파일 확장자는 <code>.yaml</code>, <code>.yml</code>, <code>.json</code>을 사용하세요.</p><h4>주요 필드</h4><ul><li><b>name</b>, <b>db_type</b>은 필수입니다.</li><li><b>db_type</b>: postgresql, mysql, mariadb, mssql, oracle, sqlite, db2, bigquery</li><li>비밀번호·인증서·개인키는 <code>${'{'}환경변수명{'}'}</code>으로 작성할 수 있습니다.</li><li>SSH 설정은 <code>ssh.enabled</code>, <code>ssh.host</code>, <code>ssh.port</code> 형식으로 작성합니다.</li><li>Import 미리보기에는 비밀번호와 키가 표시되지 않습니다.</li></ul><h4>YAML 예시</h4><pre className="guide-code">{yamlExample}</pre><h4>JSON 예시</h4><pre className="guide-code">{jsonExample}</pre></div></Modal>
}

function SourceImportDialog({ onClose, onDone }: { onClose: () => void; onDone: (message: string) => void }) {
  const [file, setFile] = useState<File|null>(null); const [preview, setPreview] = useState<any>(null); const [duplicate, setDuplicate] = useState('skip'); const [busy, setBusy] = useState(false); const [error, setError] = useState('')
  const request = async (path: string) => { if (!file) throw new Error('Import할 YAML 또는 JSON 파일을 선택하세요.'); const form = new FormData(); form.append('file', file); const token = localStorage.getItem('metavault_token'); const response = await fetch(`${path}${path.endsWith('/import')?`?duplicate=${duplicate}`:''}`, { method:'POST', headers: token ? { Authorization: `Bearer ${token}` } : {}, body: form }); const body = await response.json().catch(()=>({})); if (!response.ok) throw new Error(body.detail || 'Import 요청에 실패했습니다.'); return body }
  const selectFile = async (value: File|null) => { setFile(value); setPreview(null); setError(''); if (!value) return; setBusy(true); try { setPreview(await request('/api/sources/import/preview')) } catch(e) { setError((e as Error).message) } finally { setBusy(false) } }
  const submit = async () => { setBusy(true); setError(''); try { const result=await request('/api/sources/import'); onDone(`DB 접속 Import 완료: ${result.created}개 등록, ${result.updated}개 수정, ${result.skipped}개 건너뜀`) } catch(e) { setError((e as Error).message) } finally { setBusy(false) } }
  return <Modal title="DB 접속 일괄 Import" subtitle="YAML 또는 JSON 파일을 미리 확인한 뒤 등록합니다." onClose={onClose} wide><div className="import-content"><label className="file-drop"><Upload size={22}/><b>{file ? file.name : 'YAML 또는 JSON 파일을 선택하세요'}</b><small>최대 2MB · .yaml, .yml, .json</small><input type="file" accept=".yaml,.yml,.json,application/json,text/yaml" onChange={e=>selectFile(e.target.files?.[0]||null)}/></label>{busy&&!preview&&<Loading/>}{preview&&<><div className="import-summary"><b>{preview.total}개 항목</b><span className="tag">정상 {preview.valid}개</span>{preview.errors.length>0&&<span className="tag error-tag">오류 {preview.errors.length}개</span>}</div>{preview.items.length>0&&<div className="import-preview"><table><thead><tr><th>이름</th><th>DB 종류</th><th>호스트</th><th>포트</th><th>데이터베이스</th><th>SSH</th></tr></thead><tbody>{preview.items.map((item:any)=><tr key={`${item.name}-${item.host}`}><td>{item.name}</td><td>{item.db_type}</td><td>{item.host}</td><td>{item.port||'—'}</td><td>{item.database||'—'}</td><td>{item.ssh_enabled?'사용':'—'}</td></tr>)}</tbody></table></div>}{preview.errors.length>0&&<div className="import-errors">{preview.errors.map((item:any)=><p key={item.row}>행 {item.row}: {item.error}</p>)}</div>}<label>중복 이름 처리<select value={duplicate} onChange={e=>setDuplicate(e.target.value)}><option value="skip">건너뛰기</option><option value="overwrite">기존 연결 덮어쓰기</option><option value="rename">이름 뒤에 번호 붙이기</option></select></label></>}</div>{error&&<div className="alert"><CircleAlert size={16}/>{error}</div>}<div className="modal-actions"><button type="button" className="ghost" onClick={onClose}>취소</button><button type="button" className="primary" disabled={!preview||!preview.valid||busy} onClick={submit}>{busy&&<RefreshCw className="spin" size={16}/>}Import 실행</button></div></Modal>
}

function Sources({ toast }: { toast: (m: string) => void }) {
  const { data, loading, reload } = useLoad<Source[]>('/api/sources', [], [])
  const capabilities = useLoad<Capabilities>('/api/capabilities', { deployment_mode: 'local', supported_db_types: Object.keys(dbNames), excluded_db_types: [] }, [])
  const [open, setOpen] = useState(false); const [editing, setEditing] = useState<Source|null>(null); const [importOpen, setImportOpen] = useState(false); const [guideOpen, setGuideOpen] = useState(false); const [configOpen, setConfigOpen] = useState(false); const [query, setQuery] = useState(''); const [dbFilter, setDbFilter] = useState('all')
  const filtered = data.filter(s => (dbFilter === 'all' || s.db_type === dbFilter) && `${s.name} ${s.host} ${s.database}`.toLowerCase().includes(query.toLowerCase()))
  const test = async (id: number) => { try { await api(`/api/sources/${id}/test`, { method: 'POST' }); toast('데이터베이스 연결에 성공했습니다.'); reload() } catch(e) { toast((e as Error).message) } }
  const remove = async (id: number) => { if (!confirm('데이터 소스와 연결된 수집 작업을 삭제할까요?')) return; await api(`/api/sources/${id}`, { method: 'DELETE' }); reload() }
  return <><PageHead eyebrow="DATA MANAGEMENT" title="DB 접속 관리" description="메타데이터를 수집할 데이터베이스 연결과 인증 방식을 관리합니다." action={<div className="source-actions"><button className="ghost" onClick={()=>setConfigOpen(true)}><Database size={16}/>메타 조회 설정</button><button className="ghost" onClick={()=>setGuideOpen(true)}><BookOpen size={16}/>Import 가이드</button><button className="ghost" onClick={()=>setImportOpen(true)}><Upload size={16}/>일괄 Import</button><button className="primary" onClick={() => { setEditing(null); setOpen(true) }}><Plus size={17}/>새 연결</button></div>}/>
    <MetaTableConfigPanel sources={data} toast={toast} open={configOpen} onClose={()=>setConfigOpen(false)}/><section className="panel"><div className="toolbar"><div className="search"><Search size={17}/><input placeholder="연결 이름, 호스트 검색" value={query} onChange={e => setQuery(e.target.value)}/></div><label className="source-filter"><span>DB 종류</span><select value={dbFilter} onChange={e => setDbFilter(e.target.value)}><option value="all">전체 DB 종류</option>{capabilities.data.supported_db_types.filter(type => dbNames[type]).map(type => <option value={type} key={type}>{dbNames[type]}</option>)}</select></label><span className="count">총 {filtered.length}개 연결</span></div>{loading ? <Loading/> : filtered.length ? <div className="cards">{filtered.map(s => <article className="source-card" key={s.id}><div className="source-top"><div className={`db-logo db-${s.db_type}`}>{dbNames[s.db_type]?.slice(0,2) || 'DB'}</div><div><h3>{s.name}</h3><p>{dbNames[s.db_type]}</p></div><Status value={s.status}/></div><dl><div><dt>엔드포인트</dt><dd>{s.db_type === 'sqlite' ? s.database : `${s.host}:${s.port}`}</dd></div><div><dt>데이터베이스</dt><dd>{s.database || '—'}</dd></div><div><dt>인증</dt><dd>{s.ssh_enabled ? <><KeyRound size={14}/> SSH {s.ssh_auth_type === 'private_key' ? '개인키' : '비밀번호'}</> : <><LockKeyhole size={14}/> DB 인증</>}</dd></div><div><dt>최근 테스트</dt><dd>{fmt(s.last_tested_at)}</dd></div></dl><div className="card-actions"><button onClick={() => { setEditing(s); setOpen(true) }}><Pencil size={15}/>수정</button><button onClick={() => test(s.id)}><Zap size={15}/>연결 테스트</button><button className="danger-icon" onClick={() => remove(s.id)} title="삭제"><Trash2 size={16}/></button></div></article>)}</div> : <Empty title="등록된 데이터베이스가 없습니다" text="새 연결을 추가하면 스키마 메타데이터 수집을 시작할 수 있습니다."/>}</section>{open && <SourceForm source={editing || undefined} supportedDbTypes={capabilities.data.supported_db_types.filter(type => dbNames[type])} onClose={() => { setOpen(false); setEditing(null) }} onDone={() => { setOpen(false); setEditing(null); reload(); toast(editing ? '데이터베이스 연결을 수정했습니다.' : '데이터베이스 연결을 저장했습니다.') }}/>} {importOpen&&<SourceImportDialog onClose={()=>setImportOpen(false)} onDone={message=>{setImportOpen(false);reload();toast(message)}}/>} {guideOpen&&<ImportGuide onClose={()=>setGuideOpen(false)}/>}</>
}

function MetaTableConfigPanel({ sources, toast, open, onClose }: { sources: Source[]; toast: (message: string) => void; open: boolean; onClose: () => void }) {
  const { data, loading } = useLoad<any>('/api/meta-table-config', { source_type: 'internal', external_source_id: null, schema_name: 'EAPET', tables_table_name: 'TB_META_TABLES_EXT', columns_table_name: 'TB_META_COLUMNS_EXT' }, [])
  const [form, setForm] = useState(data)
  useEffect(() => { if (!loading) setForm(data) }, [data, loading])
  const save = async (event: FormEvent) => { event.preventDefault(); try { await api('/api/meta-table-config', { method: 'PUT', body: JSON.stringify({ ...form, external_source_id: form.source_type === 'external' && form.external_source_id ? Number(form.external_source_id) : null }) }); toast('메타 테이블 조회 설정을 저장했습니다.') } catch (error) { toast((error as Error).message) } }
  if (!open) return null
  return <Modal title="메타 테이블 조회 설정" subtitle="ETL 생성에 사용할 조회 DB와 스키마·테이블명을 지정합니다." onClose={onClose}><form className="form-grid" onSubmit={save}><label>조회 DB<select value={form.source_type} onChange={e => setForm({ ...form, source_type: e.target.value, external_source_id: null })}><option value="internal">내부 MetaVault DB</option><option value="external">외부 DB 접속정보</option></select></label>{form.source_type === 'external' && <label>외부 DB<select required value={form.external_source_id || ''} onChange={e => setForm({ ...form, external_source_id: e.target.value })}><option value="">외부 DB를 선택하세요</option>{sources.map(source => <option value={source.id} key={source.id}>{source.name} · {dbNames[source.db_type] || source.db_type}</option>)}</select></label>}<label>스키마명<input required value={form.schema_name} onChange={e => setForm({ ...form, schema_name: e.target.value })}/></label><label>테이블명<input required value={form.tables_table_name} onChange={e => setForm({ ...form, tables_table_name: e.target.value })}/></label><label>컬럼 테이블명<input required value={form.columns_table_name} onChange={e => setForm({ ...form, columns_table_name: e.target.value })}/></label><div className="form-actions"><button className="primary" type="submit">설정 저장</button></div></form></Modal>}


const COLLECTION_OPTIONS: [string, string][] = [['INDEX','인덱스'],['TABLE','테이블'],['VIEW','뷰'],['PROCEDURE','프로시저'],['SELECT PRIVILEGE','조회 권한'],['TRIGGER','트리거'],['TABLE PARTITION','테이블 파티션'],['INDEX PARTITION','인덱스 파티션'],['TABLE SUBPARTITION','테이블 서브파티션'],['INDEX SUBPARTITION','인덱스 서브파티션'],['MVIEW','Materialized View'],['SEQUENCE','시퀀스'],['DATABASE LINK','Database Link'],['SYNONYM','Synonym']]

function JobForm({ sources, onClose, onDone, job }: { sources: Source[]; onClose: () => void; onDone: () => void; job?: Job }) {
  const [form, setForm] = useState<any>(() => job ? {...job, collection_items: job.collection_items?.length ? job.collection_items : COLLECTION_OPTIONS.map(([key])=>key)} : { name: '', data_source_id: sources[0]?.id || '', schedule_type: 'cron', cron: '0 2 * * *', interval_minutes: 60, schemas: [], collection_items: ['INDEX','TABLE','VIEW','PROCEDURE','SELECT PRIVILEGE'], collect_storage: false, is_active: true }); const [schemaOptions,setSchemaOptions]=useState<string[]>([]); const [schemaLoading,setSchemaLoading]=useState(false); const [error,setError]=useState('')
  useEffect(()=>{if(!form.data_source_id){setSchemaOptions([]);return}setSchemaLoading(true);api<string[]>(`/api/sources/${form.data_source_id}/schemas`).then(setSchemaOptions).catch(()=>setSchemaOptions([])).finally(()=>setSchemaLoading(false))},[form.data_source_id])
  const submit = async (e: FormEvent) => { e.preventDefault(); try { await api(job ? `/api/jobs/${job.id}` : '/api/jobs',{method:job?'PUT':'POST',body:JSON.stringify(form)}); onDone() } catch(e){setError((e as Error).message)} }

  return <Modal title={job?'수집 스케줄 수정':'수집 스케줄 생성'} subtitle="실행 주기와 수집할 메타데이터 범위를 지정하세요." onClose={onClose}><form onSubmit={submit}><div className="form-section"><div className="form-grid one"><label>작업 이름<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="예: 운영계 매일 수집"/></label><label>데이터 소스<select required value={form.data_source_id} onChange={e=>setForm({...form,data_source_id:Number(e.target.value)})}><option value="">선택하세요</option>{sources.map(s=><option value={s.id} key={s.id}>{s.name}</option>)}</select></label><label>수집 옵션<select value={form.collect_storage?'storage':'basic'} onChange={e=>setForm({...form,collect_storage:e.target.value==='storage'})}><option value="basic">기본 메타데이터 수집</option><option value="storage">기본 + 스토리지 증가량 수집</option></select><small>스토리지 옵션은 테이블·인덱스 용량과 이전 수집 대비 증감량을 기록합니다.</small></label><fieldset className="collection-items"><legend>수집 객체 유형</legend><div className="collection-item-actions"><button type="button" className="ghost" onClick={()=>setForm({...form,collection_items:COLLECTION_OPTIONS.map(([key])=>key)})}>전체 선택</button><button type="button" className="ghost" onClick={()=>setForm({...form,collection_items:[]})}>전체 해제</button></div><div className="item-check-grid">{COLLECTION_OPTIONS.map(([key,label])=><label className="check" key={key}><input type="checkbox" checked={form.collection_items.includes(key)} onChange={e=>setForm({...form,collection_items:e.target.checked?[...form.collection_items,key]:form.collection_items.filter((x:string)=>x!==key)})}/><span><Check/></span>{label}</label>)}</div><small>선택한 객체만 다음 수집부터 저장합니다. 파티션 등은 DB 종류에 따라 결과가 없을 수 있습니다.</small></fieldset><label>실행 방식<select value={form.schedule_type} onChange={e=>setForm({...form,schedule_type:e.target.value})}><option value="cron">Cron 스케줄</option><option value="interval">주기 실행</option><option value="manual">수동 실행만</option></select></label>{form.schedule_type==='cron'&&<label>Cron 표현식<input required value={form.cron} onChange={e=>setForm({...form,cron:e.target.value})}/><small>분 시 일 월 요일 — 예: 매일 새벽 2시 = 0 2 * * *</small></label>}{form.schedule_type==='interval'&&<label>실행 간격 (분)<input type="number" min="1" value={form.interval_minutes} onChange={e=>setForm({...form,interval_minutes:Number(e.target.value)})}/></label>}<fieldset className="schema-selection"><legend>대상 스키마 (선택)</legend><div className="collection-item-actions"><button type="button" className="ghost" disabled={!schemaOptions.length} onClick={()=>setForm({...form,schemas:[...schemaOptions]})}>전체 선택</button><button type="button" className="ghost" disabled={!form.schemas.length} onClick={()=>setForm({...form,schemas:[]})}>전체 해제</button></div>{schemaLoading?<small>스키마 목록을 불러오는 중...</small>:schemaOptions.length?<div className="item-check-grid">{schemaOptions.map(schema=><label className="check" key={schema}><input type="checkbox" checked={form.schemas.includes(schema)} onChange={e=>setForm({...form,schemas:e.target.checked?[...form.schemas,schema]:form.schemas.filter((x:string)=>x!==schema)})}/><span><Check/></span>{schema}</label>)}</div>:<small>조회 가능한 스키마가 없습니다. 데이터 소스 연결 상태와 권한을 확인하세요.</small>}<small>선택하지 않으면 시스템 스키마를 제외한 전체 스키마를 수집합니다.</small></fieldset></div></div>{error&&<div className="alert">{error}</div>}<div className="modal-actions"><button type="button" className="ghost" onClick={onClose}>취소</button><button className="primary">스케줄 저장</button></div></form></Modal>
}

function JobImportGuide({ onClose }: { onClose: () => void }) {
  const example = `jobs:
  - name: 운영 DB 매일 수집
    data_source: 운영 PostgreSQL
    schedule_type: cron
    cron: "0 2 * * *"
    schemas:
      - public
    collection_items:
      - TABLE
      - VIEW
      - INDEX
      - SELECT PRIVILEGE
    collect_storage: true
    is_active: true`
  const jsonExample = `{
  "jobs": [
    {
      "name": "운영 DB 매일 수집",
      "data_source": "운영 PostgreSQL",
      "schedule_type": "cron",
      "cron": "0 2 * * *",
      "schemas": ["public"],
      "collection_items": ["TABLE", "VIEW", "INDEX", "SELECT PRIVILEGE"],
      "collect_storage": true,
      "is_active": true
    }
  ]
}`
  return <Modal title="수집 스케줄 Import 가이드" subtitle="YAML 또는 JSON으로 여러 스케줄을 한 번에 등록합니다." onClose={onClose} wide><div className="guide-content"><h4>필수 필드</h4><ul><li><b>name</b>: 수집 작업 이름</li><li><b>data_source</b>: DB 접속 관리에 등록된 연결 이름 또는 <b>data_source_id</b></li><li><b>schedule_type</b>: cron, interval, manual 중 하나</li><li><b>cron</b>: cron 방식의 실행 표현식</li><li><b>interval_minutes</b>: interval 방식의 실행 간격(분)</li></ul><h4>YAML 예시</h4><pre className="guide-code">{example}</pre><h4>JSON 예시</h4><pre className="guide-code">{jsonExample}</pre><p>중복 이름은 Import 화면에서 건너뛰기, 덮어쓰기, 이름 변경 중 선택합니다. 수집 항목은 현재 지원되는 작업 옵션을 그대로 사용합니다.</p></div></Modal>
}

function JobImportDialog({ onClose, onDone }: { onClose: () => void; onDone: (message: string) => void }) {
  const [file,setFile]=useState<File|null>(null); const [preview,setPreview]=useState<any>(null); const [duplicate,setDuplicate]=useState('skip'); const [busy,setBusy]=useState(false); const [error,setError]=useState('')
  const request=async(path:string)=>{if(!file)throw new Error('Import할 YAML 또는 JSON 파일을 선택하세요.');const form=new FormData();form.append('file',file);const token=localStorage.getItem('metavault_token');const url=`${path}${path.endsWith('/import')?`?duplicate=${duplicate}`:''}`;const response=await fetch(url,{method:'POST',headers:token?{Authorization:`Bearer ${token}`}:{},body:form});const body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body.detail||'Import 요청에 실패했습니다.');return body}
  const selectFile=async(value:File|null)=>{setFile(value);setPreview(null);setError('');if(!value)return;setBusy(true);try{setPreview(await request('/api/jobs/import/preview'))}catch(e){setError((e as Error).message)}finally{setBusy(false)}}
  const submit=async()=>{setBusy(true);setError('');try{const result=await request('/api/jobs/import');onDone(`스케줄 Import 완료: ${result.created}개 등록, ${result.updated}개 수정, ${result.skipped}개 건너뜀`)}catch(e){setError((e as Error).message)}finally{setBusy(false)}}
  return <Modal title="수집 스케줄 일괄 Import" subtitle="YAML 또는 JSON 파일을 미리 확인한 뒤 등록합니다." onClose={onClose} wide><div className="import-content"><label className="file-drop"><Upload size={22}/><b>{file?file.name:'YAML 또는 JSON 파일을 선택하세요'}</b><small>최대 2MB · .yaml, .yml, .json</small><input type="file" accept=".yaml,.yml,.json,application/json,text/yaml" onChange={e=>selectFile(e.target.files?.[0]||null)}/></label>{busy&&!preview&&<Loading/>}{preview&&<><div className="import-summary"><b>{preview.total}개 스케줄</b><span className="tag">정상 {preview.valid}개</span>{preview.errors.length>0&&<span className="tag error-tag">오류 {preview.errors.length}개</span>}</div>{preview.items.length>0&&<div className="import-preview"><table><thead><tr><th>작업 이름</th><th>데이터 소스</th><th>방식</th><th>Cron</th><th>수집 항목</th></tr></thead><tbody>{preview.items.map((item:any)=><tr key={`${item.name}-${item.data_source}`}><td>{item.name}</td><td>{item.data_source}</td><td>{item.schedule_type}</td><td>{item.cron||'—'}</td><td>{item.collection_items?.join(', ')||'—'}</td></tr>)}</tbody></table></div>}{preview.errors.length>0&&<div className="import-errors">{preview.errors.map((item:any)=><p key={item.row}>행 {item.row}: {item.error}</p>)}</div>}<label>중복 이름 처리<select value={duplicate} onChange={e=>setDuplicate(e.target.value)}><option value="skip">건너뛰기</option><option value="overwrite">기존 스케줄 덮어쓰기</option><option value="rename">이름 뒤에 번호 붙이기</option></select></label></>}</div>{error&&<div className="alert"><CircleAlert size={16}/>{error}</div>}<div className="modal-actions"><button type="button" className="ghost" onClick={onClose}>취소</button><button type="button" className="primary" disabled={!preview||!preview.valid||busy} onClick={submit}>{busy&&<RefreshCw className="spin" size={16}/>}Import 실행</button></div></Modal>
}

function RunLogDialog({ run, jobName, onClose }: { run: Run; jobName: string; onClose: () => void }) {
  const { data, loading } = useLoad<import('./types').RunLog[]>(`/api/runs/${run.id}/logs`, [], [run.id])
  return <Modal title="수집 실행 상세" subtitle={`${jobName} · ${fmt(run.started_at)}`} onClose={onClose} wide><div className="run-detail-summary"><Status value={run.status}/><b>{run.object_count}개 객체</b>{run.error_message && <p className="error-text">{run.error_message}</p>}</div>{loading ? <Loading/> : data.length ? <div className="run-log-list">{data.map(log => <div className={`run-log run-log-${log.level}`} key={log.id}><span className="run-log-sequence">{log.sequence}</span><div><b>{log.step}</b><p>{log.message}</p>{log.details && <pre>{log.details}</pre>}<small>{fmt(log.created_at)}</small></div></div>)}</div> : <p className="inline-empty">기록된 상세 로그가 없습니다.</p>}</Modal>
}

function Jobs({ toast }: { toast: (m:string)=>void }) {
  const jobs=useLoad<Job[]>('/api/jobs',[],[]), sources=useLoad<Source[]>('/api/sources',[],[]), runs=useLoad<Run[]>('/api/runs',[],[]); const [open,setOpen]=useState(false); const [editing,setEditing]=useState<Job|null>(null); const [selectedRun,setSelectedRun]=useState<Run|null>(null); const [importOpen,setImportOpen]=useState(false); const [guideOpen,setGuideOpen]=useState(false)
  const sourceName=(id:number)=>sources.data.find(s=>s.id===id)?.name||`#${id}`
  const run=async(id:number)=>{await api(`/api/jobs/${id}/run`,{method:'POST'});toast('수집 작업을 시작했습니다.');setTimeout(runs.reload,900)}
  const remove=async(id:number)=>{if(confirm('이 수집 작업과 실행 기록을 삭제할까요?')){await api(`/api/jobs/${id}`,{method:'DELETE'});jobs.reload()}}
  return <><PageHead eyebrow="AUTOMATION" title="수집 스케줄" description="스키마 수집 작업의 실행 주기와 결과를 관리합니다." action={<div className="source-actions"><button className="ghost" onClick={()=>setGuideOpen(true)}><BookOpen size={16}/>Import 가이드</button><button className="ghost" disabled={!sources.data.length} onClick={()=>setImportOpen(true)}><Upload size={16}/>일괄 Import</button><button className="primary" disabled={!sources.data.length} onClick={()=>setOpen(true)}><Plus size={17}/>스케줄 생성</button></div>}/><section className="panel table-panel"><div className="responsive-table"><table><thead><tr><th>작업 이름</th><th>데이터 소스</th><th>수집 옵션</th><th>스케줄</th><th>다음 실행</th><th>상태</th><th></th></tr></thead><tbody>{jobs.data.map(j=><tr key={j.id}><td><b>{j.name}</b><small>스키마 {j.schemas.length?j.schemas.join(', '):'전체'}</small></td><td>{sourceName(j.data_source_id)}</td><td><span className="tag">{j.collect_storage?'스토리지 증가량':'기본'}</span>{j.collection_items?.length>0&&<small className="job-items">{j.collection_items.join(', ')}</small>}</td><td><code>{j.schedule_type==='cron'?j.cron:j.schedule_type==='interval'?`${j.interval_minutes}분마다`:'수동'}</code></td><td>{fmt(j.next_run_at)}</td><td><span className={`status ${j.is_active?'status-success':'status-unchecked'}`}><i/>{j.is_active?'활성':'비활성'}</span></td><td className="row-actions"><button title="수정" onClick={()=>{setEditing(j);setOpen(true)}}><Pencil size={15}/></button><button title="지금 실행" onClick={()=>run(j.id)}><Play size={15}/></button><button title="삭제" onClick={()=>remove(j.id)}><Trash2 size={15}/></button></td></tr>)}</tbody></table>{!jobs.data.length&&<Empty title="수집 스케줄이 없습니다" text={sources.data.length?'첫 수집 스케줄을 생성해 보세요.':'먼저 데이터베이스 연결을 등록해 주세요.'} icon={CalendarClock}/>}</div></section><section className="panel"><PanelHead title="실행 이력" text="최근 100건" action={<button className="icon-btn" onClick={runs.reload}><RefreshCw size={16}/></button>}/><div className="run-list compact">{runs.data.map(r=><button className="run-row run-row-button" key={r.id} onClick={()=>setSelectedRun(r)}><div className="run-dot"><Activity size={15}/></div><div><b>{jobs.data.find(j=>j.id===r.job_id)?.name||`작업 #${r.job_id}`}</b><p>{fmt(r.started_at)} · {r.object_count}개 객체 {r.error_message&&`· ${r.error_message}`}</p></div><Status value={r.status}/></button>)}{!runs.data.length&&<p className="inline-empty">아직 실행 이력이 없습니다.</p>}</div></section>{open&&<JobForm job={editing||undefined} sources={sources.data} onClose={()=>{setOpen(false);setEditing(null)}} onDone={()=>{setOpen(false);setEditing(null);jobs.reload();toast(editing?'수집 스케줄을 수정했습니다.':'수집 스케줄을 생성했습니다.')}}/>}{importOpen&&<JobImportDialog onClose={()=>setImportOpen(false)} onDone={message=>{setImportOpen(false);jobs.reload();toast(message)}}/>}{guideOpen&&<JobImportGuide onClose={()=>setGuideOpen(false)}/>} {selectedRun&&<RunLogDialog run={selectedRun} jobName={jobs.data.find(j=>j.id===selectedRun.job_id)?.name||`작업 #${selectedRun.job_id}`} onClose={()=>setSelectedRun(null)}/>}</>
}

const EXPORT_OPTIONS: [string, string][] = [['SUMMARY','요약'],['TABLE','테이블'],['COLUMN','컬럼'],['VIEW','뷰'],['INDEX','인덱스'],['FOREIGN KEY','외래키'],['PROCEDURE','프로시저/함수'],['SELECT PRIVILEGE','조회 권한'],['STORAGE','스토리지']]

function ExportDialog({ onClose, onDownload }: { onClose: () => void; onDownload: (items: string[]) => Promise<void> }) {
  const [items, setItems] = useState(EXPORT_OPTIONS.map(([key]) => key)); const [error, setError] = useState('')
  const submit = async () => { if (!items.length) { setError('다운로드할 항목을 하나 이상 선택하세요.'); return } try { await onDownload(items) } catch (e) { setError((e as Error).message) } }
  return <Modal title="Excel 다운로드" subtitle="다운로드할 수집 항목을 선택하세요." onClose={onClose}><div className="export-dialog"><div className="collection-item-actions"><button type="button" className="ghost" onClick={()=>setItems(EXPORT_OPTIONS.map(([key])=>key))}>전체 선택</button><button type="button" className="ghost" onClick={()=>setItems([])}>전체 해제</button></div><div className="item-check-grid">{EXPORT_OPTIONS.map(([key,label])=><label className="check" key={key}><input type="checkbox" checked={items.includes(key)} onChange={e=>setItems(e.target.checked?[...items,key]:items.filter(item=>item!==key))}/><span><Check/></span>{label}</label>)}</div>{error&&<div className="alert">{error}</div>}</div><div className="modal-actions"><button type="button" className="ghost" onClick={onClose}>취소</button><button type="button" className="primary" disabled={!items.length} onClick={submit}><Download size={16}/>다운로드</button></div></Modal>
}

function Metadata() {
  const {data,loading}=useLoad<any[]>('/api/metadata',[],[])
  const [selected,setSelected]=useState<number|null>(null)
  const [query,setQuery]=useState('')
  const [schemaFilter,setSchemaFilter]=useState<string|null>(null)
  const [kind,setKind]=useState<'tables'|'views'|'procedures'>('tables')
  const [exportOpen,setExportOpen]=useState(false)
  const [registerOpen,setRegisterOpen]=useState(false)
  const [page,setPage]=useState(1); const [pageSize,setPageSize]=useState(()=>{const saved=Number(localStorage.getItem('metavault.schemaPageSize')); return [25,50,100,200].includes(saved)?saved:50})
  const current=data.find(x=>x.id===(selected||data[0]?.id))
  const tables=useMemo(()=>current?.payload.schemas.flatMap((s:any)=>(s.tables||[]).map((t:any)=>({...t,schema:s.name})))||[],[current])
  const views=useMemo(()=>current?.payload.schemas.flatMap((s:any)=>(s.views||[]).map((v:any)=>({...v,schema:s.name})))||[],[current])
  const procedures=useMemo(()=>current?.payload.schemas.flatMap((s:any)=>(s.procedures||[]).map((p:any)=>({...p,schema:s.name})))||[],[current])
  const matches=(item:any)=>(!schemaFilter||item.schema===schemaFilter)&&`${item.schema}.${item.name}`.toLowerCase().includes(query.trim().toLowerCase())
  const filteredTables=tables.filter(matches)
  const filteredViews=views.filter(matches)
  const filteredProcedures=procedures.filter(matches)
  const visibleCount=kind==='tables'?filteredTables.length:kind==='views'?filteredViews.length:filteredProcedures.length
  const visibleItems=kind==='tables'?filteredTables:kind==='views'?filteredViews:filteredProcedures
  const pageCount=Math.max(1,Math.ceil(visibleItems.length/pageSize))
  const pagedItems=visibleItems.slice((page-1)*pageSize,page*pageSize)
  useEffect(()=>{localStorage.setItem('metavault.schemaPageSize',String(pageSize))},[pageSize])
  useEffect(()=>{setPage(1)},[query,schemaFilter,kind,current?.id])
  const visibleSchemaCount=schemaFilter?1:(current?.payload.schemas?.length||0)
  const download = async (items: string[]) => {
    if (!current) return
    const token = localStorage.getItem('metavault_token')
    const params = new URLSearchParams({ items: items.join(',') })
    const response = await fetch(`/api/metadata/${current.id}/export.xlsx?${params.toString()}`, { headers: token ? { Authorization: `Bearer ${token}` } : {} })
    if (!response.ok) throw new Error('Excel 다운로드에 실패했습니다.')
    const blob = await response.blob()
    const url = URL.createObjectURL(blob)
    const anchor = document.createElement('a')
    anchor.href = url
    anchor.download = `MetaVault_${current.payload.source || 'schema'}.xlsx`
    anchor.click()
    URL.revokeObjectURL(url)
  }
  return <><PageHead eyebrow="DATA MANAGEMENT" title="스키마 정보" description="수집된 데이터베이스 구조와 컬럼 정보를 탐색합니다." action={<div className="source-actions"><button className="ghost" disabled={!current} title={current?'선택한 수집 결과를 메타 테이블에 등록합니다.':'먼저 수집 결과를 선택하세요.'} onClick={()=>setRegisterOpen(true)}><Database size={17}/>메타 등록</button><button className="primary" disabled={!current} onClick={()=>setExportOpen(true)}><Download size={17}/>Excel 다운로드</button></div>}/><div className="metadata-layout"><aside className="panel source-tree"><h3>데이터 소스</h3>{data.map(x=><div className="source-tree-group" key={x.id}><button type="button" aria-pressed={current?.id===x.id} aria-label={`${x.payload.source} 수집 결과 선택`} className={current?.id===x.id?'active':''} onClick={()=>{setSelected(Number(x.id));setSchemaFilter(null);setKind('tables');setQuery('')}}><Database size={17}/><span><b>{x.payload.source}</b><small>{x.payload.db_type} · {fmt(x.captured_at)} · {x.payload.schemas?.length||0}개 스키마</small></span><ChevronDown size={15}/></button>{current?.id===x.id&&<div className="source-schema-list"><button type="button" className={!schemaFilter?'selected':''} onClick={()=>setSchemaFilter(null)}>전체 스키마 <span>{x.payload.schemas?.length||0}</span></button>{(x.payload.schemas||[]).map((schema:any)=><button type="button" className={schemaFilter===schema.name?'selected':''} key={schema.name} onClick={()=>setSchemaFilter(schema.name)}>{schema.name}<span>{(schema.tables?.length||0)+(schema.views?.length||0)+(schema.procedures?.length||0)}</span></button>)}</div>}</div>)}{!data.length&&!loading&&<p>수집된 정보 없음</p>}</aside><section className="panel metadata-main">{current?<><div className="toolbar"><div><h3>{current.payload.source}</h3><p>{visibleSchemaCount}개 스키마 · {filteredTables.length}개 테이블 · {filteredViews.length}개 뷰 · {filteredProcedures.length}개 프로시저{current.payload.storage_summary&&` · 총 ${fmtBytes(current.payload.storage_summary.total_bytes)} · 증감 ${fmtBytes(current.payload.storage_summary.growth_bytes)}`}</p></div><div className="search"><Search size={17}/><input aria-label="스키마 개체 검색" placeholder="스키마, 테이블, 뷰 검색" value={query} onChange={e=>setQuery(e.target.value)}/></div></div><div className="metadata-tabs" role="tablist">{([['tables','테이블',filteredTables.length],['views','뷰',filteredViews.length],['procedures','프로시저·함수',filteredProcedures.length] ] as const).map(([value,label,count])=><button role="tab" aria-selected={kind===value} className={kind===value?'active':''} onClick={()=>setKind(value)} key={value}>{label}<span>{count}</span></button>)}</div><div className="metadata-result-caption">{query.trim()?`검색 결과 ${visibleCount}개`:`전체 ${visibleCount}개`} {visibleCount>0&&kind==='tables'?' · 항목을 클릭하면 상세 정보를 볼 수 있습니다.':''}</div>{kind==='views'&&<section className="object-panel metadata-scroll-list"><h3>뷰 및 조회 권한</h3>{filteredViews.length?pagedItems.map((view:any)=><div className="object-row" key={`${view.schema}.${view.name}`}><div><b>{view.name}</b><small>{view.schema}</small></div><span className={`permission ${view.permissions?.select===null?'unchecked':view.permissions?.select?'allowed':'denied'}`}>{view.permissions?.select===null?'SELECT 권한 미수집':view.permissions?.select?'SELECT 가능':'SELECT 권한 없음'}</span></div>):<p className="empty-search">검색 조건에 맞는 뷰가 없습니다.</p>}</section>}{kind==='procedures'&&<section className="object-panel metadata-scroll-list"><h3>프로시저 및 함수</h3>{filteredProcedures.length?pagedItems.map((procedure:any)=><details className="procedure-detail" key={`${procedure.schema}.${procedure.name}`}><summary className="object-row"><div><b>{procedure.name}</b><small>{procedure.schema} · {procedure.routine_type||'ROUTINE'}</small></div><ChevronDown size={15}/></summary><pre>{procedure.definition||procedure.body||'정의 내용이 수집되지 않았습니다.'}</pre></details>):<p className="empty-search">검색 조건에 맞는 프로시저가 없습니다.</p>}</section>}{kind==='tables'&&<div className="schema-list metadata-scroll-list">{pagedItems.map((table:any)=><details key={`${table.schema}.${table.name}`}><summary><div className="table-icon"><TableProperties size={16}/></div><div><b>{table.name}</b><small>{table.schema} · {table.columns.length} columns · 인덱스 {table.indexes?.length||0}개</small></div><span className={`permission ${table.permissions?.select===null?'unchecked':table.permissions?.select?'allowed':'denied'}`}>{table.permissions?.select===null?'조회 권한 미수집':table.permissions?.select?'조회 가능':'조회 권한 없음'}</span>{table.storage?.growth_bytes!=null&&<span className={`growth ${table.storage.growth_bytes>0?'up':table.storage.growth_bytes<0?'down':''}`}>{table.storage.growth_bytes>0?'+':''}{fmtBytes(table.storage.growth_bytes)}</span>}<ChevronDown size={16}/></summary>{table.storage&&<div className="storage-bar"><span><small>데이터</small><b>{fmtBytes(table.storage.data_bytes)}</b></span><span><small>인덱스</small><b>{fmtBytes(table.storage.index_bytes)}</b></span><span><small>전체</small><b>{fmtBytes(table.storage.total_bytes)}</b></span><span><small>이전 대비</small><b>{table.storage.growth_bytes==null?'기준값':`${table.storage.growth_bytes>0?'+':''}${fmtBytes(table.storage.growth_bytes)}`}</b></span><span><small>예상 행 수</small><b>{table.storage.row_estimate?.toLocaleString()??'—'}</b></span></div>}<div className="columns"><div className="column-head"><span>컬럼</span><span>타입</span><span>NULL</span><span>키</span></div>{table.columns.map((c:any)=><div className="column-row" key={c.name}><span><b>{c.name}</b>{c.comment&&<small>{c.comment}</small>}</span><code>{c.type}</code><span>{c.nullable?'YES':'NO'}</span><span>{table.primary_key?.constrained_columns?.includes(c.name)?<span className="key"><KeyRound size={12}/>PK</span>:'—'}</span></div>)}</div>{table.indexes?.length>0&&<div className="index-list"><b>인덱스</b>{table.indexes.map((index:any)=><span key={index.name}>{index.name}{index.unique?' · UNIQUE':''}</span>)}</div>}</details>)}</div>}{<div className="pagination"><label>페이지당 표시 <select aria-label="페이지당 표시 개수" value={pageSize} onChange={e=>{setPageSize(Number(e.target.value));setPage(1)}}><option value={25}>25개</option><option value={50}>50개</option><option value={100}>100개</option><option value={200}>200개</option></select></label><button type="button" className="ghost" disabled={page<=1} onClick={()=>setPage(page-1)}>이전</button><button type="button" className="ghost" disabled={page>=pageCount} onClick={()=>setPage(page+1)}>다음</button></div>}</>:<Empty title="수집된 스키마 정보가 없습니다" text="수집 스케줄을 실행하면 데이터 자산을 여기서 탐색할 수 있습니다." icon={TableProperties}/>}</section></div>{exportOpen&&<ExportDialog onClose={()=>setExportOpen(false)} onDownload={async items=>{await download(items);setExportOpen(false)}}/>}{registerOpen&&current&&<RegisterMetadataDialog snapshot={current} onClose={()=>setRegisterOpen(false)} onDone={()=>setRegisterOpen(false)}/>}</>
}

function RegisteredMetadataPage() {
  const [page, setPage] = useState(1); const [pageSize, setPageSize] = useState(()=>{const saved=Number(localStorage.getItem('metavault.registeredPageSize')); return [25,50,100,200].includes(saved)?saved:50})
  const [selected, setSelected] = useState<any>(null); const [original, setOriginal] = useState<any>(null); const [query, setQuery] = useState(''); const [sourceFilter, setSourceFilter] = useState<string|null>(null); const [message, setMessage] = useState('')
  const path = `/api/metadata/registered?page=${page}&page_size=${pageSize}&q=${encodeURIComponent(query)}${sourceFilter ? `&instance_name=${encodeURIComponent(sourceFilter)}` : ''}`
  const { data: response, loading } = useLoad<any>(path, { items: [], total: 0, pages: 0, sources: [] }, [path])
  const data = response.items || []; const total = response.total || 0; const pages = response.pages || 0
  const sources = response.sources || []; const sourceCounts = response.source_counts || {}
  const filtered = data
  useEffect(() => { localStorage.setItem('metavault.registeredPageSize', String(pageSize)) }, [pageSize])
  useEffect(() => { setPage(1); setSelected(null) }, [query, sourceFilter])
  const choose = (table: any) => { const copy = JSON.parse(JSON.stringify(table)); setSelected(copy); setOriginal(JSON.parse(JSON.stringify(table))); setMessage('') }
  const update = (index: number, key: string, value: string) => setSelected((current: any) => ({ ...current, columns: current.columns.map((column: any, i: number) => i === index ? { ...column, [key]: ['column_id', 'data_length', 'data_precision', 'data_scale'].includes(key) ? (value === '' ? null : Number(value)) : value } : column) }))
  const save = async (event: FormEvent) => { event.preventDefault(); try { await api('/api/metadata/registered', { method: 'PUT', body: JSON.stringify(selected) }); setOriginal(JSON.parse(JSON.stringify(selected))); setMessage('등록 메타 정보를 수정했습니다.') } catch (error) { setMessage((error as Error).message) } }
  return <><PageHead eyebrow="DATA MANAGEMENT" title="등록 메타 관리" description="원천시스템별 등록 테이블과 컬럼 정보를 검색하고 수정합니다."/><div className="metadata-layout registered-page-layout"><aside className="panel source-tree"><h3>원천시스템</h3><div className="search"><Search size={16}/><input aria-label="등록 메타 검색" placeholder="시스템, OWNER, 테이블, 컬럼 검색" value={query} onChange={e=>setQuery(e.target.value)}/></div><button type="button" className={!sourceFilter?'selected':''} onClick={()=>setSourceFilter(null)}>전체 시스템 <span>{total}</span></button>{sources.map((source: string)=><button type="button" className={sourceFilter===source?'selected':''} key={source} onClick={()=>setSourceFilter(source)}><Database size={16}/><span>{source}</span><small>{sourceCounts[source] || 0}개 테이블</small></button>)}{!data.length&&!loading&&<p>등록된 메타 정보가 없습니다.</p>}</aside><section className="panel metadata-main">{selected ? <form onSubmit={save}><div className="toolbar"><div><h3>{selected.owner}.{selected.table_name}</h3><p>{selected.instance_name} · 컬럼 {selected.columns.length}개</p></div><div className="source-actions"><button type="button" className="ghost" onClick={()=>{if(JSON.stringify(selected)!==JSON.stringify(original)&&!window.confirm('수정한 내용이 저장되지 않습니다. 목록으로 돌아가시겠습니까?'))return;setSelected(null);setOriginal(null);setMessage('')}}>목록으로</button><button className="primary">수정 저장</button></div></div><div className="form-grid"><label>대상 테이블명<input value={selected.tgt_table_name || ''} onChange={e=>setSelected({...selected,tgt_table_name:e.target.value})}/></label><label>설명<input value={selected.comments || ''} onChange={e=>setSelected({...selected,comments:e.target.value})}/></label></div><div className="responsive-table"><table><thead><tr><th>컬럼명</th><th>순번</th><th>TYPE</th><th>LENGTH</th><th>PRECISION</th><th>SCALE</th><th>NULL</th><th>PK</th><th>설명</th></tr></thead><tbody>{selected.columns.map((column:any,index:number)=><tr key={column.column_name}><td>{column.column_name}</td><td><input className="numeric-input" inputMode="numeric" pattern="[0-9]*" value={column.column_id??''} onChange={e=>update(index,'column_id',e.target.value)}/></td><td><input value={column.data_type||''} onChange={e=>update(index,'data_type',e.target.value)}/></td><td><input className="numeric-input" inputMode="numeric" pattern="[0-9]*" value={column.data_length??''} onChange={e=>update(index,'data_length',e.target.value)}/></td><td><input className="numeric-input" inputMode="numeric" pattern="[0-9]*" value={column.data_precision??''} onChange={e=>update(index,'data_precision',e.target.value)}/></td><td><input className="numeric-input" inputMode="numeric" pattern="[0-9]*" value={column.data_scale??''} onChange={e=>update(index,'data_scale',e.target.value)}/></td><td><select value={column.null_yn||'Y'} onChange={e=>update(index,'null_yn',e.target.value)}><option value="Y">Y</option><option value="N">N</option></select></td><td><select value={column.pk_yn||'N'} onChange={e=>update(index,'pk_yn',e.target.value)}><option value="Y">Y</option><option value="N">N</option></select></td><td><input value={column.comments||''} onChange={e=>update(index,'comments',e.target.value)}/></td></tr>)}</tbody></table></div>{message&&<div className="alert">{message}</div>}</form> : <><div className="toolbar"><div><h3>{sourceFilter||'전체 시스템'}</h3><p>{query?`검색 결과 ${filtered.length}개`:`등록 테이블 ${filtered.length}개`}</p></div></div><div className="schema-list metadata-scroll-list">{filtered.map((table:any)=><button type="button" className="object-row" key={`${table.instance_name}.${table.owner}.${table.table_name}`} onClick={()=>choose(table)}><div><b>{table.table_name}</b><small>{table.instance_name} · {table.owner} · 컬럼 {table.columns.length}개</small></div><ChevronDown size={15}/></button>)}{!filtered.length&&!loading&&<p className="empty-search">검색 조건에 맞는 등록 메타가 없습니다.</p>}</div><div className="pagination"><label>페이지당 표시 <select aria-label="페이지당 표시 개수" value={pageSize} onChange={e=>{setPageSize(Number(e.target.value));setPage(1)}}><option value={25}>25개</option><option value={50}>50개</option><option value={100}>100개</option><option value={200}>200개</option></select></label><button type="button" className="ghost" disabled={page<=1} onClick={()=>setPage(page-1)}>이전</button><span>{pages?`${page} / ${pages} 페이지 · 총 ${total}개`:''}</span><button type="button" className="ghost" disabled={!pages||page>=pages} onClick={()=>setPage(page+1)}>다음</button></div></>}</section></div></>}

function RegisteredMetadataPageLegacy() {
  const { data, loading } = useLoad<any[]>('/api/metadata/registered', [], [])
  const [selected, setSelected] = useState<any>(null)
  const [message, setMessage] = useState('')
  const choose = (table: any) => { setSelected(JSON.parse(JSON.stringify(table))); setMessage('') }
  const update = (index: number, key: string, value: string) => setSelected((current: any) => ({ ...current, columns: current.columns.map((column: any, i: number) => i === index ? { ...column, [key]: ['column_id', 'data_length', 'data_precision', 'data_scale'].includes(key) ? (value === '' ? null : Number(value)) : value } : column) }))
  const save = async (event: FormEvent) => { event.preventDefault(); try { await api('/api/metadata/registered', { method: 'PUT', body: JSON.stringify(selected) }); setMessage('등록 메타 정보를 수정했습니다.') } catch (error) { setMessage((error as Error).message) } }
  return <><PageHead eyebrow="DATA MANAGEMENT" title="등록 메타 관리" description="EAPET 스키마에 등록된 테이블과 컬럼 정보를 조회하고 수정합니다."/><div className="registered-page-layout"><aside className="panel picker-list"><div className="picker-head"><b>등록 테이블</b><span>{data.length}개</span></div>{loading ? <Loading/> : data.map(table => <button type="button" className={selected?.table_name === table.table_name && selected?.owner === table.owner ? 'active' : ''} onClick={() => choose(table)} key={`${table.owner}.${table.table_name}`}><b>{table.table_name}</b><small>{table.owner} · 컬럼 {table.columns.length}개</small></button>)}{!loading && !data.length && <p>등록된 메타 정보가 없습니다.</p>}</aside><section className="panel registered-main">{selected ? <form onSubmit={save}><div className="toolbar"><div><h3>{selected.owner}.{selected.table_name}</h3><p>컬럼 {selected.columns.length}개 · 등록 메타 수정</p></div><button className="primary">수정 저장</button></div><div className="form-grid"><label>대상 테이블명<input value={selected.tgt_table_name || ''} onChange={e => setSelected({ ...selected, tgt_table_name: e.target.value })}/></label><label>설명<input value={selected.comments || ''} onChange={e => setSelected({ ...selected, comments: e.target.value })}/></label></div><div className="responsive-table"><table><thead><tr><th>컬럼명</th><th>순번</th><th>TYPE</th><th>LENGTH</th><th>PRECISION</th><th>SCALE</th><th>NULL</th><th>PK</th><th>설명</th></tr></thead><tbody>{selected.columns.map((column: any, index: number) => <tr key={column.column_name}><td>{column.column_name}</td><td><input className="numeric-input" inputMode="numeric" pattern="[0-9]*" value={column.column_id ?? ''} onChange={e => update(index, 'column_id', e.target.value)}/></td><td><input value={column.data_type || ''} onChange={e => update(index, 'data_type', e.target.value)}/></td><td><input className="numeric-input" inputMode="numeric" pattern="[0-9]*" value={column.data_length ?? ''} onChange={e => update(index, 'data_length', e.target.value)}/></td><td><input className="numeric-input" inputMode="numeric" pattern="[0-9]*" value={column.data_precision ?? ''} onChange={e => update(index, 'data_precision', e.target.value)}/></td><td><input className="numeric-input" inputMode="numeric" pattern="[0-9]*" value={column.data_scale ?? ''} onChange={e => update(index, 'data_scale', e.target.value)}/></td><td><select value={column.null_yn || 'Y'} onChange={e => update(index, 'null_yn', e.target.value)}><option value="Y">Y</option><option value="N">N</option></select></td><td><select value={column.pk_yn || 'N'} onChange={e => update(index, 'pk_yn', e.target.value)}><option value="Y">Y</option><option value="N">N</option></select></td><td><input value={column.comments || ''} onChange={e => update(index, 'comments', e.target.value)}/></td></tr>)}</tbody></table></div>{message && <div className="alert">{message}</div>}</form> : <Empty title="등록된 테이블을 선택하세요" text="왼쪽 목록에서 수정할 등록 메타 테이블을 선택하세요." icon={Pencil}/>}</section></div></>}

function RegisterMetadataDialog({ snapshot, onClose, onDone }: { snapshot: any; onClose: () => void; onDone: () => void }) {
  const tables = snapshot.payload.schemas.flatMap((schema: any) => (schema.tables || []).map((table: any) => ({ ...table, schema: schema.name })))
  const [selected, setSelected] = useState<string[]>([])
  const [selectedColumns, setSelectedColumns] = useState<Record<string, string[]>>({})
  const [expanded, setExpanded] = useState<string[]>([])
  const [error, setError] = useState('')
  const [form, setForm] = useState({ system_cd: '', postfix: 'N', etl_conn_div_cd: '', etl_conn_nm: '', tgt_ds_cd: '', tgt_database_name: '', instance_div_cd: '', target_name_suffix: '' })
  const set = (key: string, value: string) => setForm(current => ({ ...current, [key]: value }))
  const submit = async (event: FormEvent) => { event.preventDefault(); setError(''); if (!selected.length) { setError('등록할 테이블을 하나 이상 선택하세요.'); return } if (selected.some(name => !(selectedColumns[name]?.length))) { setError('선택한 모든 테이블에서 컬럼을 하나 이상 선택하세요.'); return } try { const result = await api<any>('/api/metadata/register', { method: 'POST', body: JSON.stringify({ snapshot_id: snapshot.id, table_names: selected, columns_by_table: selectedColumns, ...form }) }); onDone(); alert(`메타 등록 완료: 테이블 ${result.tables}개, 컬럼 ${result.columns}개`) } catch (e) { setError((e as Error).message) } }
  const toggleAll = (checked: boolean) => { setSelected(checked ? tables.map((table: any) => table.name) : []); setSelectedColumns(checked ? Object.fromEntries(tables.map((table: any) => [table.name, table.columns.map((column: any) => column.name)])) : {}) }
  const toggleTable = (table: any, checked: boolean) => { setSelected(current => checked ? [...current, table.name] : current.filter(name => name !== table.name)); setSelectedColumns(current => ({ ...current, [table.name]: checked ? table.columns.map((column: any) => column.name) : [] })) }
  const toggleColumn = (table: any, columnName: string, checked: boolean) => setSelectedColumns(current => ({ ...current, [table.name]: checked ? [...(current[table.name] || []), columnName] : (current[table.name] || []).filter(name => name !== columnName) }))
  return <Modal title="메타 테이블 등록" subtitle="수집된 테이블을 선택해 TB_META_TABLES_EXT와 TB_META_COLUMNS_EXT에 등록합니다." onClose={onClose} wide><form onSubmit={submit}><div className="form-section"><div className="form-grid"><label>시스템 코드<input required maxLength={32} value={form.system_cd} onChange={e => set('system_cd', e.target.value)}/><small>필수 · 최대 32자</small></label><label>POSTFIX<input required maxLength={32} value={form.postfix} onChange={e => set('postfix', e.target.value)}/><small>필수</small></label><label>ETL 연결 구분 코드<input required maxLength={32} value={form.etl_conn_div_cd} onChange={e => set('etl_conn_div_cd', e.target.value)}/><small>필수 · 예: Oracle, ODBC</small></label><label>ETL 연결명<input required maxLength={255} value={form.etl_conn_nm} onChange={e => set('etl_conn_nm', e.target.value)}/><small>필수</small></label><label>대상 데이터셋 코드<input required maxLength={255} value={form.tgt_ds_cd} onChange={e => set('tgt_ds_cd', e.target.value)}/><small>필수 · 최대 7자 권장</small></label><label>대상 데이터베이스명<input required maxLength={255} value={form.tgt_database_name} onChange={e => set('tgt_database_name', e.target.value)}/><small>필수</small></label><label>인스턴스 구분 코드<input maxLength={32} value={form.instance_div_cd} onChange={e => set('instance_div_cd', e.target.value)}/></label><label>대상 테이블 접미사<input maxLength={32} value={form.target_name_suffix} onChange={e => set('target_name_suffix', e.target.value)} placeholder="예: _N"/></label></div></div><div className="register-table-picker"><div className="picker-head"><b>등록할 테이블 ({selected.length}/{tables.length})</b><label className="check"><input type="checkbox" checked={selected.length === tables.length && tables.length > 0} onChange={e => toggleAll(e.target.checked)}/><span><Check/></span>전체 선택</label></div><div className="picker-list">{tables.map((table: any) => <label className="check" key={`${table.schema}.${table.name}`}><input type="checkbox" checked={selected.includes(table.name)} onChange={e => toggleTable(table, e.target.checked)}/><span><Check/></span><div><b>{table.name}</b><small>{table.schema} · 컬럼 {table.columns?.length || 0}개 · 선택 {selectedColumns[table.name]?.length || 0}개</small></div><button type="button" className="ghost" onClick={() => setExpanded(current => current.includes(table.name) ? current.filter(name => name !== table.name) : [...current, table.name])}>{expanded.includes(table.name) ? '컬럼 닫기' : '컬럼 선택'}</button>{expanded.includes(table.name) && <div className="column-picker">{table.columns.map((column: any) => <label className="check" key={column.name}><input type="checkbox" checked={(selectedColumns[table.name] || []).includes(column.name)} onChange={e => toggleColumn(table, column.name, e.target.checked)}/><span><Check/></span><div><b>{column.name}</b><small>{column.type || '—'} · {column.nullable === false ? 'NOT NULL' : 'NULL'}{column.ordinal_position ? ` · 순번 ${column.ordinal_position}` : ''}</small></div></label>)}</div>}</label>)}</div></div>{error && <div className="alert"><CircleAlert size={16}/>{error}</div>}<div className="modal-actions"><button type="button" className="ghost" onClick={onClose}>취소</button><button className="primary">선택 테이블 등록</button></div></form></Modal>}

function RegisteredMetadataDialog({ onClose, onDone }: { onClose: () => void; onDone: () => void }) {
  const { data, loading } = useLoad<any[]>('/api/metadata/registered', [], [])
  const [selected, setSelected] = useState<any>(null)
  const [error, setError] = useState('')
  const select = (table: any) => setSelected(JSON.parse(JSON.stringify(table)))
  const updateColumn = (index: number, key: string, value: string) => setSelected((current: any) => ({ ...current, columns: current.columns.map((column: any, i: number) => i === index ? { ...column, [key]: ['column_id', 'data_length', 'data_precision', 'data_scale'].includes(key) ? (value === '' ? null : Number(value)) : value } : column) }))
  const save = async (event: FormEvent) => { event.preventDefault(); setError(''); try { await api('/api/metadata/registered', { method: 'PUT', body: JSON.stringify(selected) }); onDone(); alert('등록 메타 정보를 수정했습니다.') } catch (e) { setError((e as Error).message) } }
  return <Modal title="등록 메타 관리" subtitle="EAPET 스키마에 등록된 테이블과 컬럼 정보를 수정합니다." onClose={onClose} wide>{loading ? <Loading/> : <div className="registered-editor"><aside className="picker-list">{data.map(table => <button type="button" className={selected?.table_name === table.table_name && selected?.owner === table.owner ? 'active' : ''} onClick={() => select(table)} key={`${table.owner}.${table.table_name}`}><b>{table.table_name}</b><small>{table.owner} · 컬럼 {table.columns.length}개</small></button>)}{!data.length && <p>등록된 메타 정보가 없습니다.</p>}</aside>{selected && <form onSubmit={save} className="registered-form"><div className="form-grid"><label>테이블명<input readOnly value={selected.table_name}/></label><label>OWNER<input readOnly value={selected.owner}/></label><label>대상 테이블명<input value={selected.tgt_table_name || ''} onChange={e => setSelected({ ...selected, tgt_table_name: e.target.value })}/></label><label>설명<input value={selected.comments || ''} onChange={e => setSelected({ ...selected, comments: e.target.value })}/></label></div><div className="responsive-table"><table><thead><tr><th>컬럼명</th><th>순번</th><th>TYPE</th><th>LENGTH</th><th>PRECISION</th><th>SCALE</th><th>NULL</th><th>PK</th><th>설명</th></tr></thead><tbody>{selected.columns.map((column: any, index: number) => <tr key={column.column_name}><td>{column.column_name}</td><td><input value={column.data_type || ''} onChange={e => updateColumn(index, 'data_type', e.target.value)}/></td>{(['column_id', 'data_length', 'data_precision', 'data_scale'] as const).map(key => <td key={key}><input className="numeric-input" inputMode="numeric" pattern="[0-9]*" value={column[key] ?? ''} onChange={e => updateColumn(index, key, e.target.value)}/></td>)}<td><select value={column.null_yn || ''} onChange={e => updateColumn(index, 'null_yn', e.target.value)}><option value="Y">Y</option><option value="N">N</option></select></td><td><select value={column.pk_yn || ''} onChange={e => updateColumn(index, 'pk_yn', e.target.value)}><option value="Y">Y</option><option value="N">N</option></select></td><td><input value={column.comments || ''} onChange={e => updateColumn(index, 'comments', e.target.value)}/></td></tr>)}</tbody></table></div>{error && <div className="alert"><CircleAlert size={16}/>{error}</div>}<div className="modal-actions"><button type="button" className="ghost" onClick={onClose}>취소</button><button className="primary">수정 저장</button></div></form>}</div>}</Modal>}

function PasswordChange({ onClose }: { onClose: () => void }) {
  const [form, setForm] = useState({ current_password: '', new_password: '', confirm_password: '' })
  const [error, setError] = useState('')
  const [busy, setBusy] = useState(false)
  const submit = async (e: FormEvent) => {
    e.preventDefault(); setError('')
    if (form.new_password !== form.confirm_password) { setError('새 비밀번호가 일치하지 않습니다.'); return }
    setBusy(true)
    try { await api('/api/auth/password', { method: 'POST', body: JSON.stringify({ current_password: form.current_password, new_password: form.new_password }) }); onClose() }
    catch (e) { setError((e as Error).message) }
    finally { setBusy(false) }
  }
  return <Modal title="비밀번호 변경" subtitle="현재 비밀번호를 확인한 뒤 새 비밀번호로 변경합니다." onClose={onClose}><form onSubmit={submit}><div className="form-section"><div className="form-grid one"><label>현재 비밀번호<input required type="password" value={form.current_password} onChange={e=>setForm({...form,current_password:e.target.value})}/></label><label>새 비밀번호<input required minLength={8} type="password" value={form.new_password} onChange={e=>setForm({...form,new_password:e.target.value})}/><small>8자 이상 입력하세요.</small></label><label>새 비밀번호 확인<input required minLength={8} type="password" value={form.confirm_password} onChange={e=>setForm({...form,confirm_password:e.target.value})}/></label></div></div>{error&&<div className="alert"><CircleAlert size={16}/>{error}</div>}<div className="modal-actions"><button type="button" className="ghost" onClick={onClose}>취소</button><button className="primary" disabled={busy}>{busy&&<RefreshCw className="spin" size={16}/>}비밀번호 변경</button></div></form></Modal>
}

function UserForm({roles,onClose,onDone}:{roles:Role[];onClose:()=>void;onDone:()=>void}){const[form,setForm]=useState<any>({email:'',name:'',password:'',is_active:true,role_ids:[]});const submit=async(e:FormEvent)=>{e.preventDefault();await api('/api/admin/users',{method:'POST',body:JSON.stringify(form)});onDone()};return <Modal title="사용자 추가" onClose={onClose}><form onSubmit={submit}><div className="form-section"><div className="form-grid one"><label>이름<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>이메일<input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>초기 비밀번호<input required type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label><fieldset><legend>역할</legend>{roles.map(r=><label className="check" key={r.id}><input type="checkbox" onChange={e=>setForm({...form,role_ids:e.target.checked?[...form.role_ids,r.id]:form.role_ids.filter((x:number)=>x!==r.id)})}/><span><Check/></span>{r.name}</label>)}</fieldset></div></div><div className="modal-actions"><button type="button" className="ghost" onClick={onClose}>취소</button><button className="primary">사용자 저장</button></div></form></Modal>}

function UsersPage({toast}:{toast:(m:string)=>void}){const users=useLoad<User[]>('/api/admin/users',[],[]),roles=useLoad<Role[]>('/api/admin/roles',[],[]);const[open,setOpen]=useState(false);const remove=async(id:number,name:string)=>{if(!confirm(`${name} 사용자를 삭제할까요? 삭제한 사용자는 복구할 수 없습니다.`))return;try{await api(`/api/admin/users/${id}`,{method:'DELETE'});users.reload();toast('사용자를 삭제했습니다.')}catch(e){toast((e as Error).message)}};return <><PageHead eyebrow="ADMINISTRATION" title="사용자 관리" description="관리 콘솔에 접근할 사용자와 활성 상태를 관리합니다." action={<button className="primary" onClick={()=>setOpen(true)}><Plus size={17}/>사용자 추가</button>}/><section className="panel table-panel"><div className="responsive-table"><table><thead><tr><th>사용자</th><th>역할</th><th>상태</th><th>등록일</th><th></th></tr></thead><tbody>{users.data.map(u=><tr key={u.id}><td><div className="user-cell"><span>{u.name.slice(0,1)}</span><div><b>{u.name}</b><small>{u.email}</small></div></div></td><td>{u.roles.map(r=><span className="tag" key={r}>{r}</span>)}</td><td><span className={`status ${u.is_active?'status-success':'status-failed'}`}><i/>{u.is_active?'활성':'중지'}</span></td><td>—</td><td className="row-actions"><button title="사용자 삭제" onClick={()=>remove(u.id,u.name)}><Trash2 size={15}/></button></td></tr>)}</tbody></table></div></section>{open&&<UserForm roles={roles.data} onClose={()=>setOpen(false)} onDone={()=>{setOpen(false);users.reload();toast('사용자를 추가했습니다.')}}/>}</>}

function RoleForm({permissions,menus,onClose,onDone}:{permissions:Permission[];menus:Menu[];onClose:()=>void;onDone:()=>void}){const[form,setForm]=useState<any>({name:'',description:'',permission_ids:[],menu_ids:[]});const toggle=(key:string,id:number,on:boolean)=>setForm({...form,[key]:on?[...form[key],id]:form[key].filter((x:number)=>x!==id)});const submit=async(e:FormEvent)=>{e.preventDefault();await api('/api/admin/roles',{method:'POST',body:JSON.stringify(form)});onDone()};return <Modal title="새 역할 만들기" subtitle="기능 권한과 노출할 메뉴를 함께 지정합니다." onClose={onClose} wide><form onSubmit={submit}><div className="form-section"><div className="form-grid"><label>역할 이름<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>설명<input value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></label></div></div><div className="permission-grid"><fieldset><legend>기능 권한</legend>{permissions.map(p=><label className="check permission" key={p.id}><input type="checkbox" onChange={e=>toggle('permission_ids',p.id,e.target.checked)}/><span><Check/></span><div><b>{p.code}</b><small>{p.description}</small></div></label>)}</fieldset><fieldset><legend>노출 메뉴</legend>{menus.map(m=><label className="check permission" key={m.id}><input type="checkbox" onChange={e=>toggle('menu_ids',m.id,e.target.checked)}/><span><Check/></span><div><b>{m.label}</b><small>{m.path}</small></div></label>)}</fieldset></div><div className="modal-actions"><button type="button" className="ghost" onClick={onClose}>취소</button><button className="primary">역할 저장</button></div></form></Modal>}

function MenuForm({onClose,onDone}:{onClose:()=>void;onDone:()=>void}){const[form,setForm]=useState({code:'',label:'',path:'/',icon:'Circle',order:10,parent_id:null});const submit=async(e:FormEvent)=>{e.preventDefault();await api('/api/admin/menus',{method:'POST',body:JSON.stringify(form)});onDone()};return <Modal title="메뉴 추가" subtitle="역할에 배정할 관리 콘솔 메뉴를 등록합니다." onClose={onClose}><form onSubmit={submit}><div className="form-section"><div className="form-grid"><label>메뉴 코드<input required value={form.code} onChange={e=>setForm({...form,code:e.target.value})} placeholder="audit"/></label><label>메뉴명<input required value={form.label} onChange={e=>setForm({...form,label:e.target.value})} placeholder="감사 로그"/></label><label>경로<input required value={form.path} onChange={e=>setForm({...form,path:e.target.value})} placeholder="/audit"/></label><label>정렬 순서<input type="number" value={form.order} onChange={e=>setForm({...form,order:Number(e.target.value)})}/></label></div></div><div className="modal-actions"><button type="button" className="ghost" onClick={onClose}>취소</button><button className="primary">메뉴 저장</button></div></form></Modal>}

function RolesPage({toast}:{toast:(m:string)=>void}){const roles=useLoad<Role[]>('/api/admin/roles',[],[]),permissions=useLoad<Permission[]>('/api/admin/permissions',[],[]),menus=useLoad<Menu[]>('/api/admin/menus',[],[]);const[open,setOpen]=useState(false);const[menuOpen,setMenuOpen]=useState(false);return <><PageHead eyebrow="ADMINISTRATION" title="역할 및 권한" description="역할별 기능 권한과 접근 가능한 메뉴를 구성합니다." action={<button className="primary" onClick={()=>setOpen(true)}><Plus size={17}/>역할 추가</button>}/><div className="role-grid">{roles.data.map(r=><article className="panel role-card" key={r.id}><div className="role-icon"><ShieldCheck size={22}/></div><h3>{r.name}</h3><p>{r.description||'설명 없음'}</p><div className="role-counts"><span><b>{r.permissions.length}</b> 기능 권한</span><span><b>{r.menus.length}</b> 접근 메뉴</span></div><div className="role-tags">{r.permissions.slice(0,4).map(id=><span className="tag" key={id}>{permissions.data.find(p=>p.id===id)?.code}</span>)}{r.permissions.length>4&&<span className="tag">+{r.permissions.length-4}</span>}</div></article>)}</div><section className="panel menu-panel"><PanelHead title="메뉴 구성" text="역할별로 노출할 메뉴 목록" action={<button className="ghost" onClick={()=>setMenuOpen(true)}><Plus size={15}/>메뉴 추가</button>}/><div className="menu-list">{menus.data.map(m=><div key={m.id}><span>{m.order}</span><div><b>{m.label}</b><small>{m.code}</small></div><code>{m.path}</code></div>)}</div></section>{open&&<RoleForm permissions={permissions.data} menus={menus.data} onClose={()=>setOpen(false)} onDone={()=>{setOpen(false);roles.reload();toast('새 역할을 만들었습니다.')}}/>}{menuOpen&&<MenuForm onClose={()=>setMenuOpen(false)} onDone={()=>{setMenuOpen(false);menus.reload();toast('메뉴를 추가했습니다.')}}/>}</>}

function Loading(){return <div className="loading"><RefreshCw className="spin"/>불러오는 중...</div>}

export default function App() {
  const [user,setUser]=useState<User|null>(null); const [checking,setChecking]=useState(true); const [page,setPage]=useState<Page>(()=>(location.hash.slice(1) as Page)||'dashboard'); const [mobile,setMobile]=useState(false); const [toast,setToast]=useState(''); const [passwordOpen,setPasswordOpen]=useState(false)
  useEffect(()=>{const token=localStorage.getItem('metavault_token');if(!token){setChecking(false);return}api<User>('/api/auth/me').then(setUser).catch(()=>localStorage.removeItem('metavault_token')).finally(()=>setChecking(false))},[])
  useEffect(()=>{location.hash=page},[page]); if(checking)return <Loading/>; if(!user)return <Login onLogin={setUser}/>
  const visible=nav.filter(n=>user.menus.includes(n.id)||user.permissions.includes('admin:*')||user.roles.includes('시스템 관리자')); const select=(p:Page)=>{setPage(p);setMobile(false)}; const groups=[...new Set(visible.map(n=>n.group))]
  const logout=()=>{localStorage.removeItem('metavault_token');setUser(null)}
  return <div className="app-shell"><aside className={`sidebar ${mobile?'mobile-open':''}`}><div className="brand"><div className="brand-mark"><Database size={21}/></div><b>Meta<span>Vault</span></b><button className="sidebar-close" onClick={()=>setMobile(false)}><X/></button></div><nav>{groups.map(group=><div className="nav-group" key={group}><p>{group}</p>{visible.filter(n=>n.group===group).map(item=><button className={page===item.id?'active':''} onClick={()=>select(item.id)} key={item.id}><item.icon size={18}/>{item.label}</button>)}</div>)}</nav><div className="sidebar-foot"><div className="user-mini"><span>{user.name.slice(0,1)}</span><div><b>{user.name}</b><small>{user.roles[0]||'사용자'}</small></div><button onClick={logout} title="로그아웃"><LogOut size={16}/></button></div></div></aside>{mobile&&<div className="mobile-overlay" onClick={()=>setMobile(false)}/>}<main className="workspace"><div className="topbar"><button className="mobile-menu" onClick={()=>setMobile(true)}><MenuIcon/></button><div className="system-state"><i/>시스템 정상</div><div className="top-user"><span>{user.name.slice(0,1)}</span>{user.name}<button className="ghost password-button" onClick={()=>setPasswordOpen(true)} title="비밀번호 변경"><KeyRound size={15}/>비밀번호 변경</button></div></div><div className="content">{page==='dashboard'&&<Dashboard go={select}/>} {page==='sources'&&<Sources toast={setToast}/>} {page==='jobs'&&<Jobs toast={setToast}/>} {page==='metadata'&&<Metadata/>} {page==='registered'&&<RegisteredMetadataPage/>} {page==='users'&&<UsersPage toast={setToast}/>} {page==='roles'&&<RolesPage toast={setToast}/>}</div></main>{toast&&<Toast message={toast} onClose={()=>setToast('')}/>} {passwordOpen&&<PasswordChange onClose={()=>setPasswordOpen(false)}/>}</div>
}
